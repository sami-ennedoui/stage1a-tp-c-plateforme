#include <stdio.h>

int main(void) {
    int lignes, colonnes;

    printf("Nombre de lignes : ");
    scanf("%d", &lignes);
    printf("Nombre de colonnes : ");
    scanf("%d", &colonnes);

    /* À TOI. Avec deux boucles for imbriquées, affiche un rectangle fait du
       caractère '-'. La boucle extérieure compte les lignes. La boucle
       intérieure affiche une ligne de tirets, puis tu passes à la ligne. */

    return 0;
}
