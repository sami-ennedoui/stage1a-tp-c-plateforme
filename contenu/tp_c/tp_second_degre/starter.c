#include <stdio.h>
#include <math.h>

int main(void) {
    double a, b, c;

    printf("Equation a*x*x + b*x + c = 0\n");
    printf("a = ");
    scanf("%lf", &a);
    printf("b = ");
    scanf("%lf", &b);
    printf("c = ");
    scanf("%lf", &c);

    /* À TOI. Calcule le discriminant delta = b*b - 4*a*c. Selon son signe,
       affiche les deux solutions réelles, la solution double, ou l'absence de
       solution réelle. La fonction sqrt te donne la racine carrée. */

    return 0;
}
