#include <stdio.h>

int main(void) {
    float a = 1.5, b = 0.5, c = 0.6;

    printf("Valeurs : a = %f, b = %f, c = %f\n", a, b, c);

    /* À TOI. Affiche un menu, lis le choix de l'utilisateur avec scanf, et avec
       un switch case modifie a, b ou c selon le choix. Si le choix n'existe pas,
       affiche un message d'erreur. Affiche ensuite les nouvelles valeurs. Puis,
       avec une boucle do while, redemande s'il veut modifier autre chose. */

    return 0;
}
