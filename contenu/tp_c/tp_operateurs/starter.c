#include <stdio.h>

int main(void) {
    int a = 17;
    int b = 21;

    printf("a = %d, b = %d\n", a, b);

    /* À TOI. Affiche les résultats des opérateurs, un par ligne, sous la forme
       "a & b = ...".
       Relationnels : a > b, a < b, a == b, a != b.
       Logiques : a && b, a || b, !a, !b.
       Bit à bit : a & b, a | b, a ^ b.
       Sers-toi de printf avec %d pour chaque résultat. */

    return 0;
}
