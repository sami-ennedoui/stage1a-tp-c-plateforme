#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* Exercice 12 du BE : calcul des parametres d'un cercle a partir du rayon. */

/* A toi de definir la constante PI avec #define. */

/* A toi de definir le type type_cercle avec typedef struct :
   quatre champs float rayon, diametre, aire, perimetre. */

/* SP_SAISIE_RAYON(float* R) : demande le rayon au clavier, redemande
   tant qu'il est <= 0. */

/* SP_CALCUL_RAYON(type_cercle* c) : remplit diametre, aire et perimetre
   a partir du rayon. */

/* SP_AFFICH_PARAM(type_cercle c) : affiche les quatre champs, avec
   %.1f pour le diametre, %.2f pour le perimetre, %.3f pour l'aire. */

int main(void)
{
    /* Declare un cercle, saisis le rayon, calcule puis affiche. */
    return 0;
}
