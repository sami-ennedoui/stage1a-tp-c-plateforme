/* S3. Écris maximum : parcours le tableau et renvoie son plus grand élément. */
#include "s3_tableaux.h"

int maximum(const int tab[], int n) {
    /* À TOI. Pars du premier élément, tab[0], comme meilleur candidat. Puis
       parcours les autres avec une boucle. Dès qu'un élément est plus grand, il
       devient le nouveau meilleur. tab[i] te donne l'élément numéro i. */
    return 0;
}
