/* S1. Range n dans un char et renvoie ce qu'on relit. */
#include "s1_types.h"

int valeur_dans_char(int n) {
    /* À TOI. Déclare une variable de type char, range n dedans, puis renvoie-la.
       Un char ne garde que huit bits, donc pour un grand n la valeur change.
       Le sujet le montre, 320 rangé dans un char se relit 64. */
    return 0;
}
