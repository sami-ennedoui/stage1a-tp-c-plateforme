/* S1. Écris moyenne pour qu'elle renvoie la vraie moyenne, un réel. */
#include "s1_types.h"

double moyenne(int a, int b, int c) {
    /* À TOI. La somme de trois entiers divisée par 3 ne donne pas toujours un
       entier. Et si tu divises deux entiers, le C jette la partie après la
       virgule. Trouve comment forcer le calcul à se faire en réel. */
    return 0.0;
}
