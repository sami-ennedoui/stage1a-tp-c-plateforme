__pycache__/
*.pyc
progression.json
.superpowers/
projet-corrige/SNAKE/snake
*.o
projet-corrige/tests_logique/test_init
projet-corrige/tests_logique/test_deplacement
projet-corrige/tests_logique/test_croissance
projet-corrige/tests_logique/test_collision
# Espaces de travail projet des tests (vivent hors dépôt, en /tmp)
# Aucun binaire ni copie de l'espace projet ne doit être suivi ici.
projet-squelette/SNAKE/snake
# copie de travail vivante du parcours projet, remplie par l'étudiant au lancement
espace_session/

# artefact ecrit par l exercice 10 (ecriture fichier) quand sa porte tourne
resultats.txt

# compagnon LTI : environnement virtuel et base locale, hors depot
compagnon/.venv/
compagnon/*.sqlite3

# mode auteur : empreinte du mot de passe, locale a chaque poste
auteur.json

# reglages locaux (dernier parcours), locaux a chaque poste
reglages.json

# compilateur portable embarque a cote de l'appli, telecharge, hors depot
w64devkit/

# serveur de langage embarque (diagnostics en direct), telecharge par
# packaging/build_windows.ps1, 63 Mo : jamais dans le depot
clangd/

# relevé de progression écrit par --releve, artefact local à l'étudiant
releve.txt

# traces de session ecrites par journal_session.py (donnees de demo, hors depot)
journaux/

# outillage de build (packaging/build_windows.ps1) : sorties locales, hors depot
/w64devkit/
/.build/
/_bundle/

# appairage et file Moodle, propre a chaque poste
moodle_sync.json
